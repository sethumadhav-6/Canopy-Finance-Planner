# Desktop dev + Android runtime deps (both, since buildozer reads requirements from buildozer.spec's
# `requirements =` line, not this file -- this file is for `pip install -r requirements.txt` when
# running/testing on your desktop machine).
kivy==2.3.0
kivymd==1.2.0
pytest==8.3.3
